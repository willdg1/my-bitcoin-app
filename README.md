# If doing manually in terminal:
git init
git add .
git commit -m "Initial commit: Bitcoin L1 social app via OP_NET"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/my-bitcoin-app.git
git push -u origin main
